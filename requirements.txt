# Framework web minimalista para Python
# Usado para crear la aplicación web, rutas, sesiones y manejo de templates HTML
Flask==3.0.0

# Adaptador PostgreSQL para Python
# Permite conectar y comunicarse con la base de datos PostgreSQL
psycopg2-binary==2.9.9

# Herramienta para cargar variables de entorno desde un archivo .env
# Permite manejar credenciales de bases de datos de forma segura sin hardcodearlas
python-dotenv==1.0.0

# Librería WSGI utilitaria incluida con Flask
# Proporciona funciones de seguridad como hash de contraseñas (generate_password_hash, check_password_hash)
Werkzeug==3.0.1

